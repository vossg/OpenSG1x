SET(OSG_BISON_EXE "@BISON_EXE@")

IF(OSG_BISON_EXE)
  GET_FILENAME_COMPONENT(OSG_BISON_DIR ${OSG_BISON_EXE} DIRECTORY)

  SET(ENV{PATH} ${OSG_BISON_DIR})

  EXECUTE_PROCESS(COMMAND ${OSG_BISON_EXE} -d -v -p${YY_OPT} -o ${YY_SRC} --defines=${YY_HDR} ${YY_FILE})

ENDIF()
